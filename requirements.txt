# --- Web app (Flask) ---
Flask==3.0.3
Flask-Login==0.6.3
Flask-WTF==1.2.2
WTForms==3.1.2
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.36
itsdangerous==2.2.0
email-validator==2.2.0

# --- Streamlit (para painel/verificação no Streamlit Cloud) ---
streamlit==1.49.1

# --- Deploy server (apenas fora do Windows) ---
gunicorn==22.0.0 ; platform_system != "Windows"

# --- Postgres driver (apenas fora do Windows; use se definir DATABASE_URL) ---
psycopg2-binary==2.9.9 ; platform_system != "Windows"
