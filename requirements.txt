# --- Web app (Flask) ---
Flask==3.0.3
Flask-Login==0.6.3
Flask-WTF==1.2.2
WTForms==3.1.2
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.36
itsdangerous==2.2.0
email-validator==2.2.0
Jinja2==3.1.4
Werkzeug==3.1.3

# --- Streamlit (somente para painel/verificação no Streamlit Cloud) ---
streamlit==1.49.1

# --- Deploy server (não usado no Streamlit, mas útil para Render/Railway; evita instalar no Windows) ---
gunicorn==22.0.0 ; platform_system != "Windows"

# NÃO instalar driver de Postgres aqui.
# Se um dia for usar Postgres no Streamlit, troque para o arquivo da opção B.
